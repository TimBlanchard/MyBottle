<div class="text-center">
  <h1> MyBottle </h1>
</div>

<div class="container">
  <div class="row">
    <% @articles.each do |article| %>
      <div class="col-12">
      <%= link_to article_path(article.id) do %>
        <div class="shadow bg-white rounded p-4 d-flex justify-content-between mb-4">
          <h2><%= article.title %> - <%= "🌠" * article.stars %></h2>
          <p><b><%= article.created_at.strftime("%D") %></b></p>
        </div>
      <% end %>
        <%= link_to "Créer un nouvel article", new_article_path, class: "btn btn-light" %>
      </div>
    <% end %>
  </div>
</div>


